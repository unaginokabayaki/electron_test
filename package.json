{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "electron ./src",
    "build-win": "electron-packager ./src test --platform=win32 --arch=x64 --overwrite",
    "build-mac": "electron-packager ./src test --platform=darwin --arch=x64 --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.0.4",
    "electron-packager": "^13.0.1"
  }
}
